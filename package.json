{
  "name": "asterion-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test harness for the asterion built-in UI (vanilla ES modules).",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^2.1.8"
  }
}
