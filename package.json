{
  "name": "adminfoundry-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test harness for the adminfoundry built-in UI (vanilla ES modules).",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
